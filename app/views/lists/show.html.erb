<div class='banner'>
<h1><%= @list.name %></h1>
</div>

<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li><%= bookmark.movie.title %>
    <%= link_to 'Remove a movie', bookmark_path(bookmark.id), data: { turbo_method: 'delete' } %>
    </li>
    <%= bookmark.comment %>
  <% end %>
</ul>

<%= link_to 'Add a movie', new_list_bookmark_path(@list.id) %>
<%= link_to '<< Back', root_path %>
